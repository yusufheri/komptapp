<form method="post" action="{{ path('eny_banking_info_delete', {'id': eny_banking_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eny_banking_info.id) }}">
    <button class="btn btn-outline-danger"> <i class="fa fa-trash"></i> Supprimer ce compte</button>
</form>
