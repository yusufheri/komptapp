{% extends 'base.html.twig' %}

{% block title %}Tranche{% endblock %}

{% block body %}
    <h1>Tranche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tranche.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ tranche.createdAt ? tranche.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ tranche.updatedAt ? tranche.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ tranche.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tranche.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tranche_index') }}">back to list</a>

    <a href="{{ path('tranche_edit', {'id': tranche.id}) }}">edit</a>

    {{ include('tranche/_delete_form.html.twig') }}
{% endblock %}
