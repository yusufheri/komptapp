{% extends 'base.html.twig' %}

{% block title %}EnyBank{% endblock %}

{% block body %}
    <h1>EnyBank</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eny_bank.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ eny_bank.createdAt ? eny_bank.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eny_bank.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ eny_bank.content }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ eny_bank.tel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ eny_bank.email }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ eny_bank.website }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ eny_bank.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eny_bank_index') }}">back to list</a>

    <a href="{{ path('eny_bank_edit', {'id': eny_bank.id}) }}">edit</a>

    {{ include('eny_bank/_delete_form.html.twig') }}
{% endblock %}
