{% extends 'base.html.twig' %}

{% block title %}Tranche::komptApp{% endblock %}

{% block body %}
    <h1>Tranche #{{ eny_tranche.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CreatedAt</th>
                <td>{{ eny_tranche.createdAt ? eny_tranche.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ eny_tranche.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eny_tranche.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eny_tranche_index') }}">back to list</a>

    <a href="{{ path('eny_tranche_edit', {'id': eny_tranche.id}) }}">edit</a>

    {{ include('eny_tranche/_delete_form.html.twig') }}
{% endblock %}
