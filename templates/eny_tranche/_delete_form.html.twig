<form method="post" action="{{ path('eny_tranche_delete', {'id': eny_tranche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eny_tranche.id) }}">
    <button class="btn">Delete</button>
</form>
