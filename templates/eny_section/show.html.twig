{% extends 'base.html.twig' %}

{% block title %}EnySection{% endblock %}

{% block body %}
    <h1>EnySection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eny_section.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ eny_section.createdAt ? eny_section.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eny_section.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ eny_section.content }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ eny_section.deletedAt ? eny_section.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eny_section_index') }}">back to list</a>

    <a href="{{ path('eny_section_edit', {'id': eny_section.id}) }}">edit</a>

    {{ include('eny_section/_delete_form.html.twig') }}
{% endblock %}
