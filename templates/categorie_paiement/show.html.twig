{% extends 'base.html.twig' %}

{% block title %}CategoriePaiement{% endblock %}

{% block body %}
    <h1>CategoriePaiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_paiement.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ categorie_paiement.createdAt ? categorie_paiement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ categorie_paiement.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_paiement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_paiement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_paiement_index') }}">back to list</a>

    <a href="{{ path('categorie_paiement_edit', {'id': categorie_paiement.id}) }}">edit</a>

    {{ include('categorie_paiement/_delete_form.html.twig') }}
{% endblock %}
